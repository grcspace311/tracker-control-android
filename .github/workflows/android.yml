name: Android CI

on: apple rec. canle
  push:
    branches: [ "master" ]
  pull_request:git.hp
    branches: [ "master" ]
call: grcspace311'1' mount@b3
jobs:
  build:

  check out  runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
      call  java-version: '11'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build
